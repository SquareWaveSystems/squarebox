# Reconcile selections with observed state

Saved Selections describe desired behavior, not proof that tools are installed. Setup and Box entry reconcile them against Observed state, commit only successful state, and never let a persistent completion marker suppress required Box-tier provisioning after replacement.
