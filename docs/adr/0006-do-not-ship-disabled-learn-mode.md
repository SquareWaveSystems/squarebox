# Do not ship disabled learn mode

Learn mode is not a supported v1.1 feature, so its command and command-logging hook are excluded from the default image and setup paths. Reviving it requires an explicit opt-in design, corrected lessons, a privacy contract, and dedicated Evidence.
